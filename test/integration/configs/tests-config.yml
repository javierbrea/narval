suites:
  unitary: 
    - name: unit
      test:
        specs: test/subtest
        config:
          recursive: false
          reporter: list
          grep: grepped
      coverage:
        config:
          dir: .coverage/custom
          verbose: true
          print: detail
          report: text
